"use client";
import React from "react";
import classes from "@/styles/page.module.css";

const Page = () => {
  return (
    <div className={classes["container"]}>
      <div className={classes["box"]}>Stake stream</div>
    </div>
  );
};

export default Page;
